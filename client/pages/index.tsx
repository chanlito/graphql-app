import { component } from 'vue-tsx-support';
import { VContainer } from 'vuetify-tsx';

export default component({
  render() {
    return (
      <VContainer fluid>
        Home!!!
      </VContainer>
    );
  },
});
